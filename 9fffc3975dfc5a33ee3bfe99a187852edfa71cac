WA-ATS-01 review: PASS — 7c8f467 replaces the regex split + shlex in scripts/bounded_runs.py with a quote-, escape-, comment- and heredoc-aware Scanner, so a heavy-run word counts only in command position; $(...), backticks (also inside double quotes), subshells, { }, if/while/do and a heredoc fed to a bare shell are still scanned. Title fully met: heredoc containing lizard passes, bare lizard is refused, both at matcher and end-to-end hook level. Gate (via ka0s-bounded): python3 scripts/test_bounded_runs.py 20/20 OK (16 -> 20; the four new tests fail against master's matcher and the 16 old ones pass, verified in a throwaway worktree); plugin.json, marketplace.json, hooks.json parse; all touched files LF (i/lf w/lf, 0 CR); largest authored file 632 lines (agents/review.md), none over 1500. Ripple complete: CLAUDE.md and README.md hook paragraphs updated, module docstring updated, 9fffc39 (WA-ATS-01R) drops shlex from both DEPENDENCIES.md rows and the :29-32 import citation resolves. Adversarial probe of 38 shapes: new matcher additionally catches for/while/do, !, then; newly passes $(cat <<EOF ...), sequential heredocs, escaped \; and unterminated quotes (correct). One non-blocking new false negative: 'cat <<EOF | bash' with a heavy body is no longer caught (old matcher caught it by accident); falls under the documented 'false negatives on exotic shell are acceptable'. Pre-existing gaps unchanged (sudo/xargs prefixes). No version bump, consistent with the sweep.
