WA-ATS-01 review: FAIL — The matcher change itself is correct and meets the item title: scripts/bounded_runs.py's quote- and heredoc-aware Scanner lets heredoc bodies and quoted prose (including the git commit -m "$(cat <<'EOF' ...)" pattern) pass, while a bare lizard run is still denied; command substitutions, backticks, subshells, bash <<EOF, { }, if/do/! prefixes and 2>&1 & are all still caught. Gate (bounded): python3 scripts/test_bounded_runs.py ran 20 tests, OK (16 before), both manifests parse, the four touched files are LF in the index and the working tree, and the largest authored file is agents/review.md at 632 lines, under 1500. A probe of 39 commands against the old matcher showed only improvements or acceptable false negatives. One false negative worth noting: cat <<EOF | bash (a heredoc piped to a shell) is no longer caught. The fail is for missed ripple: DEPENDENCIES.md:37 and :83 still list shlex as an import, but the commit removed it, and DEPENDENCIES.md:37 still cites scripts/bounded_runs.py:27-31 for the imports, which are now at :29-32. Fix it in a WA-ATS-01R commit that drops shlex from both rows and re-points the citation to :29-32.
